#include<iostream>
using namespace std;
void modulomulinverse(int a,int b)
{
    if(gcd(a,b)==1)
}
int main()
{
    cout<<"enter the numbers. a and b";
    cin>>a>>b;
    modulomulinverse(a,b);
    return 0;
}
